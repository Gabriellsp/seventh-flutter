name: seventh_prova_flutter
description: A new Flutter project.

publish_to: "none" 
version: 1.0.0+1

environment:
  sdk: ">=2.18.4 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  mobx: ^2.0.1 
  flutter_mobx: ^2.0.0
  flutter_modular: ^5.0.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
  build_runner:
    ^2.1.2 
  mobx_codegen:
    ^2.0.1+3 

flutter:
  uses-material-design: true
